# Security: Force patched versions for vulnerable dependencies
# https://pnpm.io/pnpmfile#hooks
overrides.js-yaml=^4.0.0
overrides.postcss=^8.5.10

# Engine strict mode for Node/pnpm version enforcement
engine-strict=true
