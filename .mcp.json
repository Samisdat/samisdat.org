{
  "mcpServers": {
    "chrome-devtools": {
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest", "--isolated"],
      "description": "Official Chrome DevTools MCP server (puppeteer-based). Inspect/interact with Chrome, performance traces, network & console. Toggle with .claude/chrome-mcp.sh on|off."
    }
  }
}
