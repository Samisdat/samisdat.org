var gitcommit = {
        task: {
            options: {
                message: 'Add github charts data as json',
                noVerify: true,
                noStatus: false
            },
            files: {
                src: ['json/*']
            }
        }
    };

module.exports = gitcommit;
